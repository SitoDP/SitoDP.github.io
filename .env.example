VITE_SCRIPT_URL=your_google_apps_script_url_here

# Google Sheets published-as-CSV URL for editable calculator config (detailing + logistics prices)
# Sheets → File → Share → Publish to web → select the config sheet → CSV → copy URL
VITE_CALC_CONFIG_URL=
